import React from "react";
import { DrawerItemList } from "@react-navigation/drawer";

const VideoPlayerSidebarLinks = (props) => {
  return <DrawerItemList {...props} />;
};

export default VideoPlayerSidebarLinks;
