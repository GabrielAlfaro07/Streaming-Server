import React from "react";
import { DrawerItemList } from "@react-navigation/drawer";

const AudioPlayerSidebarLinks = (props) => {
  return <DrawerItemList {...props} />;
};

export default AudioPlayerSidebarLinks;
